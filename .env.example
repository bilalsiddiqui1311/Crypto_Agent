# Optional OpenAI-backed suggestions. Without this key, the backend uses rule-based analysis.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini

# Local Docker defaults
FRONTEND_PORT=3000
POSTGRES_PORT=5433
AUTH_SECRET=change-this-local-secret
